apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingapi
  namespace: pingpong
spec:
  selector:
    matchLabels:
      name: ping
      tier: frontend
  replicas: 1
  template:
    metadata:
      labels:
        name: ping
        tier: frontend
    spec:
      containers:
      - name: ping
        image: kumsub/ping:v1
        ports:
        - name: tcp
          containerPort: 8090
        env:
        - name: SERVER_PORT
          value: "8090"
        - name: CACHE_TYPE
          value: redis
        - name: CACHE_HOST
          value: redis
        - name: CACHE_PORT
          value: "6379"
